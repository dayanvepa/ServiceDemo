name: CI and CD Pipeline

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
  pull_request:
    branches:
      - main  
env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: us-central1
  SERVICE_NAME: mi-app
  REPOSITORY: mi-app
  SONAR_PROJECT_KEY: ${{ vars.SONAR_PROJECT_KEY }}
  SONAR_ORGANIZATION: ${{ vars.SONAR_ORGANIZATION }}
  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

jobs:
  # ─────────────────────────────────────────
  # JOB: CI — Integración Continua
  # ─────────────────────────────────────────
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
      security-events: write
    steps:

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Necesario para sonar-scanner que requiere el historial completo

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      # SonarCloud solo corre en push a ramas y pull_request (NO en tags)
      - name: Install SonarScanner
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: dotnet tool install --global dotnet-sonarscanner

      - name: Add .NET tools to PATH
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: echo "$HOME/.dotnet/tools" >> $GITHUB_PATH

      - name: Limpiar Solución
        run: dotnet clean ServiceDemo.slnx --configuration Release

      - name: Begin Sonar Analysis
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: |
          dotnet sonarscanner begin \
            /k:"${{ env.SONAR_PROJECT_KEY }}" \
            /o:"${{ env.SONAR_ORGANIZATION }}" \
            /d:sonar.token="${{ env.SONAR_TOKEN }}" \
            /d:sonar.host.url="https://sonarcloud.io" \
            /d:sonar.qualitygate.wait=true \
            /d:sonar.cs.opencover.reportsPaths="./TestResults/coverage/coverage.opencover.xml" \
            /d:sonar.exclusions="**/Migrations/**,**/obj/**,**/bin/**,**/Program.cs,**/DependencyInjection.cs,src/ServiceDemo.Domain/**,src/ServiceDemo.Infrastructure/Data/ServiceDemoDbContext.cs,src/ServiceDemo.Application/Mappings/**" \
            /d:sonar.coverage.exclusions="**/Program.cs,**/DependencyInjection.cs,src/ServiceDemo.Domain/**,src/ServiceDemo.Infrastructure/**,src/ServiceDemo.API/**,src/ServiceDemo.Application/Validators/**,src/ServiceDemo.Application/Common/**"

      - name: Restaurar Dependencias
        run: dotnet restore ServiceDemo.slnx

      - name: Compilar Solución
        run: dotnet build ServiceDemo.slnx --no-restore --configuration Release

      - name: Run Tests with Coverage
        run: |
          dotnet test ServiceDemo.slnx \
            --no-build \
            --configuration Release \
            --collect:"XPlat Code Coverage" \
            --results-directory ./TestResults \
            -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=opencover

      - name: Verificar y mover archivo de cobertura
        run: |
          echo "Archivos encontrados:"
          find ./TestResults -name "*.xml" -type f
          mkdir -p ./TestResults/coverage
          find ./TestResults -name "coverage.opencover.xml" -exec cp {} ./TestResults/coverage/coverage.opencover.xml \;
          echo "Archivo copiado:"
          ls -la ./TestResults/coverage/

      - name: End Sonar Analysis
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: dotnet sonarscanner end /d:sonar.token="${{ env.SONAR_TOKEN }}"

      # Build y push de imagen solo en push (no en pull_request)
      - name: Google Auth
        if: github.event_name == 'push'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      # Calcula la versión:
      # - Si es un tag v1.2.3 → usa "1.2.3"
      # - Si es un push normal → usa el SHA del commit
      - name: Calcular versión semántica
        if: github.event_name == 'push'
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION="${{ github.ref_name }}"
            VERSION="${VERSION#v}"
            IS_RELEASE="true"
          else
            VERSION="${{ github.sha }}"
            IS_RELEASE="false"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "is_release=$IS_RELEASE" >> $GITHUB_OUTPUT
          echo "Versión calculada: $VERSION (release: $IS_RELEASE)"

      - name: Build Local Image
        if: github.event_name == 'push'
        run: |
          docker build -t local_image_to_scan:${{ steps.version.outputs.version }} .

      # ── Escaneo de vulnerabilidades con Trivy (instalación manual verificada) ──
      - name: Install Trivy
        if: github.event_name == 'push'
        run: |
          set -euo pipefail

          TRIVY_VERSION="0.71.1"
          DEB_FILE="trivy_${TRIVY_VERSION}_Linux-64bit.deb"
          CHECKSUMS="trivy_${TRIVY_VERSION}_checksums.txt"
          BASE_URL="https://github.com/aquasecurity/trivy/releases/download/v${TRIVY_VERSION}"

          echo "Descargando Trivy v${TRIVY_VERSION}..."
          curl -fsSL --location \
            --proto '=https' --proto-redir '=https' \
            --retry 3 --retry-all-errors \
            -o "${DEB_FILE}" "${BASE_URL}/${DEB_FILE}"

          curl -fsSL --location \
            --proto '=https' --proto-redir '=https' \
            --retry 3 --retry-all-errors \
            -o "${CHECKSUMS}" "${BASE_URL}/${CHECKSUMS}"

          echo "Verificando integridad del paquete..."
          grep -F " ${DEB_FILE}" "${CHECKSUMS}" | sha256sum -c -
          
          echo "Instalando Trivy..."
          sudo dpkg -i "${DEB_FILE}"
          
          rm -f "${DEB_FILE}" "${CHECKSUMS}"
          trivy version
          
      - name: Scan Docker image with Trivy
        if: github.event_name == 'push'
        run: |
          echo "Iniciando escaneo de vulnerabilidades..."
          trivy image \
            --severity CRITICAL,HIGH \
            --ignore-unfixed \
            --scanners vuln \
            --exit-code 1 \
            --format table \
            local_image_to_scan:${{ steps.version.outputs.version }}

      - name: Push Container to Artifact Registry
        if: github.event_name == 'push'
        run: |
          BASE_URL="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}"
          VERSION="${{ steps.version.outputs.version }}"
          
          gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev
          
          docker tag local_image_to_scan:$VERSION $BASE_URL:$VERSION
          docker push $BASE_URL:$VERSION
          
          if [ "${{ steps.version.outputs.is_release }}" = "true" ]; then
            docker tag $BASE_URL:$VERSION $BASE_URL:latest
            docker push $BASE_URL:latest
          fi

  # ─────────────────────────────────────────
  # JOB: CD — Despliegue Continuo en GKE
  # ─────────────────────────────────────────
  cd:
    needs: ci
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Google Auth (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          install_components: 'gke-gcloud-auth-plugin'

      - name: Get GKE Credentials
        uses: google-github-actions/get-gke-credentials@v2
        with:
          cluster_name: mi-cluster-autopilot
          location: ${{ env.REGION }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      # Obtener revisión activa antes de desplegar (para rollback manual si falla)
      - name: Obtener revisión activa actual
        id: current_revision
        run: |
          REVISION=$(kubectl get deployment mi-app -o jsonpath='{.metadata.annotations.deployment\.kubernetes\.io/revision}' 2>/dev/null || echo "")
          echo "revision=$REVISION" >> $GITHUB_OUTPUT
          echo "Revisión actual del deployment: $REVISION"

      # Recalcula la misma versión usada en CI
      - name: Calcular versión para deploy
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION="${{ github.ref_name }}"
            VERSION="${VERSION#v}"
          else
            VERSION="${{ github.sha }}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Desplegando versión: $VERSION"

      # ── DEPLOY TO GKE ──
      - name: Deploy to GKE
        id: deploy
        run: |
          IMAGE_URL="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${{ steps.version.outputs.version }}"
          echo "Imagen a desplegar: $IMAGE_URL"

          # Reemplaza la variable en el manifest y aplica
          sed -i "s|\${IMAGE_URL}|$IMAGE_URL|g" k8s/deployment.yaml
          kubectl apply -f k8s/

          # Espera a que el rollout termine
          kubectl rollout status deployment/mi-app --timeout=180s

      # Verificar salud del nuevo deploy
      - name: Verificar salud del deploy
        id: health_check
        run: |
          # Obtener IP externa del LoadBalancer (puede tardar unos segundos)
          for i in {1..10}; do
            SERVICE_IP=$(kubectl get svc mi-app-service -o jsonpath='{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
            [ -n "$SERVICE_IP" ] && break
            sleep 5
          done

          if [ -z "$SERVICE_IP" ]; then
            echo "No se pudo obtener la IP del servicio"
            exit 1
          fi

          echo "Verificando salud en: http://$SERVICE_IP/health"

          for i in {1..5}; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "http://$SERVICE_IP/health" || echo "000")
            echo "Intento $i — HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              echo "healthy=true" >> $GITHUB_OUTPUT
              echo "✅ Servicio saludable"
              exit 0
            fi
            sleep 10
          done

          echo "healthy=false" >> $GITHUB_OUTPUT
          echo "❌ Servicio no responde correctamente"
          exit 1

      # Rollback automático si falla
      - name: Rollback automático si falla
        if: failure() && steps.current_revision.outputs.revision != ''
        run: |
          echo "Deploy fallido. Haciendo rollback a revisión anterior..."
          kubectl rollout undo deployment/mi-app
          echo "Rollback completado."

      - name: Show Service URL
        run: |
          SERVICE_IP=$(kubectl get svc mi-app-service -o jsonpath='{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "PENDIENTE")
          echo "Deployed at: http://$SERVICE_IP"
          echo "Image version: ${{ steps.version.outputs.version }}"