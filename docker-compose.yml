services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: productservice-sql
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Chilly2025!
    ports:
      - "1433:1433"
    volumes:
      - sql_data:/var/opt/mssql
volumes:
  sql_data: